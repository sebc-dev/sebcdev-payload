# A generated secret for Payload
PAYLOAD_SECRET=

# Cloudflare Bindings Configuration
# Set to 'true' to use remote Cloudflare bindings (D1, R2) instead of local
# Useful for running migrations against production database
# Default: false (uses local Wrangler bindings)
# CLOUDFLARE_REMOTE_BINDINGS=true

# Payload Persistent Storage Path
# Directory for Wrangler's local persistent storage (D1, KV, R2)
# Default: .payload/persist (relative to src/)
# PAYLOAD_PERSIST_PATH=.payload/persist

# Live Preview Configuration
# Base URL for Live Preview iframe (used in Payload Admin)
# ⚠️ IMPORTANT: Sans trailing slash (ne pas finir par /) — ce sera copié tel quel en production
# ✅ Correct: http://localhost:3000
# ❌ Incorrect: http://localhost:3000/
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
